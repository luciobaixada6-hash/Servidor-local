
componentes:
  schemas:
    # schema/servico.yaml
    Servico:
      type: object
      properties:
        id:
          type: integer
          example: 1
        nome:
          type: string
          example: "servico sentinela"
        descricao:
          type: string
          example: "Servico de sentinela profissional para residência e trabalho"
        categoria:
          type: string
          example: "Limpeza"
        enabled:
          type: boolean
          example: true
        created_at:
          type: string
          example: "2022-01-01T00:00:00Z "
        updated_at:
          type: string
          example: "2022-01-01T00:00:00Z "

    #schema para criar um novo servico
    CreateServico:
      type: object
      properties:
        nome:
          type: string
          example: servico sentinela 
        descricao:
          type: string
          example: Servico de sentinela profissional para residência e trabalho
        categoria:
          type: string
          example: Limpeza
        enabled:
          type: boolean
          example: true
    # schema para buscar servico detalhado
    ServicoDetalhado:
      type: object
      properties:
        id:
          type: integer
          example: 1
        nome:
          type: string
          example: "servico sentinela"
        descricao:
          type: string
          example: "Servico de sentinela profissional para residência e trabalho"
        designacao_categoria:
          type: string
          example: "Limpeza"
        id_empresa:
          type: integer
          example: 1
        designacao_empresa:
          type: string
        example: "empresa sentinela"
      icone_empresa:
        type: string
        example: ""
      enabled:
          type: boolean
          example: true

      
    ResponseSuccess:
    type: object
    properties:
      message:
        type: string
        example: "Servicos encontrados com sucesso"
      status:
        type: bolean
        example: true
      data:
        type: object
        $ref: '#/components/schemas/Servico'

    # schema generico de resposta de erro
    ResponseError:
      type: object
      properties:
        message:
          type: string
          example: "Error ao buscar servicos"
        status:
          type: bolean
          example: false
        data:
          type: object
          properties:
            null

